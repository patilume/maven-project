pipeline{
    agent any
      stages {
        stage('clone'){
				step{
          git 'https://github.com/patilume/maven-project.git'
					}
		}
		stage('compile'){
			steps{
				withMaven(maven : 'maven'){
					sh 'mvn compile'
				}
			}
			
		}
	}
}
