pipeline{
    agent any
      stages {
        stage('clone'){
					steps{
          	git 'https://github.com/patilume/maven-project.git'
					}
				}
				stage('compile'){
					steps{
						withMaven(maven : 'maven'){
						sh 'mvn compile'
					}
				}
			}
			stage('test'){
				step{
					withMaven(maven : 'maven'){
						sh 'mvn test'
					}
				}
			}
	}
}
